# CLAUDE.md — BFL Creative Studio

> Generated by (SHIP), mid-project after S5. Stack verified working live this session —
> no pre-flight needed. Full PRD: Project Knowledge (claude.ai). Design System:
> docs/DESIGN_SYSTEM.md. Session history: SESSION_LOG.md.

## 1. North Star / Scope
Internal tool for Bajaj Finserv's Insta EMI Card team. Campaign manager fills 4 fields
(intent, product, offer, context) → single Anthropic API call → production-ready
7-section designer brief, so designers never need a follow-up call.

**In scope (remaining — S6/S7):**
- Visual polish to match docs/DESIGN_SYSTEM.md (light theme v1.1) exactly
- Fix CTR waterfall math consistency in lib/systemPrompt.ts
- QA public/creatives/ once 82 reference images are uploaded (user handles upload separately)
- Final polish pass + Vercel deploy + README

**Out of scope:** auth, brief history/storage, feedback loop, admin panel, mobile layout,
competitor benchmarks, email-to-designer.

## 2. Tech Stack
| Layer | Choice | Version |
|---|---|---|
| Framework | Next.js (App Router, TypeScript) | 14 |
| Styling | Tailwind CSS — CSS vars in globals.css, arbitrary-value classes (no config edits) | v3 |
| AI | @anthropic-ai/sdk, model `claude-sonnet-4-6`, single call/brief, prompt caching on system prompt | — |
| Icons | lucide-react | — |
| Fonts | next/font/google — Space Grotesk, DM Sans, JetBrains Mono | — |
| Hosting | Vercel (Azure Static Web Apps = IT fallback) | — |

All verified working end-to-end live (S5 build, this session).

## 3. Conventions
- Design tokens: CSS custom properties on `:root` in app/globals.css — see
  docs/DESIGN_SYSTEM.md §4.1–4.5 for full table (colours, spacing, radius, shadow).
- Reference tokens via Tailwind arbitrary values: `bg-[var(--c-surface)]`,
  `rounded-[var(--radius-lg)]`, `shadow-[var(--shadow-card)]`.
- Font utilities: `.font-display` (Space Grotesk), `.font-data` (JetBrains Mono).
  Body defaults to DM Sans via `--font-body` — no utility needed.
- Weight ceiling: Space Grotesk ≤600, DM Sans ≤500. Never exceed.
- Letter-spacing scale: −0.8px at 30px headline → normal (0) below 16px. Mono ALL CAPS
  labels get +0.06–0.14em (wider, not tighter).
- Shape rule: section cards/inputs/buttons = 4–6px radius (sharp). Chips/badges/pills =
  `--radius-pill` (9999px). Never blur this line.
- Components: PascalCase in /components, one component per file, `'use client'` only
  where state/effects/handlers are used.

## 4. File & Folder Structure (current — matches reality after S5)
```
app/
  layout.tsx, globals.css, page.tsx
  api/generate-brief/route.ts
components/
  BriefForm.tsx, BriefOutput.tsx, CopyButton.tsx, CreativeCard.tsx
lib/
  knowledge.ts, creativeLibrary.ts, systemPrompt.ts
types/
  index.ts
public/creatives/   ← 82 PNGs populated and verified (15 Jun)
docs/
  DESIGN_SYSTEM.md, KNOWN_ISSUES.md
```

## 5. Types / Schema / API Contracts
Fully defined in `types/index.ts` — `BriefRequest`, `BriefResponse`, `CreativeCard`,
`CreativeIntent` ('Card-Led' | 'Product-Led' | 'Offer-Led' | 'Festive/Event'), etc. Stable —
do not change without updating both `route.ts` and `BriefOutput.tsx` consumers.
`BriefResponse.reference_creatives` may return 3–5 items (LLM occasionally trims; not yet
enforced — see docs/KNOWN_ISSUES.md).

## 6. Build Session Plan
| Session | Scope | Key Output | Verification |
|---|---|---|---|
| S7A | Update systemPrompt.ts to produce v2 BriefResponse shape (scenario routing, component_spec, copy_pack, ctr_signal_analysis, etc.) | Updated lib/systemPrompt.ts | `npm run build` = 0 errors; LLM returns parseable v2 JSON |
| S7B | Rework BriefOutput.tsx to render v2 fields (scenario card, structural_keep/story_change, component_spec, copy_pack, ab_experiment, ctr_signal_analysis range) | Updated components/BriefOutput.tsx | All sections render correctly for 3 test briefs |
| S7C | Wire route.ts — validate v2 JSON shape, update BriefOutput prop type from `any` to `BriefResponse` | Updated app/api/generate-brief/route.ts, BriefOutput.tsx | `npm run build` = 0 type errors; 3 briefs end-to-end |
| S7D | Final polish + README + deploy | README.md, Vercel deploy | 3 real briefs end-to-end, 0 console errors, shared URL works |

## 7. Current Status
**v2 architecture approved, pre-flight complete. S7A next.**
KB baseline updated to 2.12% median (104 creatives). Archetype table revised with 7 rows
including Partner Co-branded and Niche Standalone. BriefResponse type replaced with v2
schema (scenario routing, component_spec, copy_pack, ctr_signal_analysis, etc.).
BriefOutput.tsx currently renders v1 fields — expected until S7B/S7C completes.
Pre-flight committed. 25 Jun 2026.

## 8. Tool Division
| | Claude Code | Cursor |
|---|---|---|
| Use for | S6a/S6b/S6c/S7 — multi-file builds, systemPrompt logic, polish passes | Image uploads to public/creatives/, git ops, `npm run dev`, one-line tweaks |
| Default if unsure | Touches >1 file or changes logic → **Claude Code** | Asset/file-management only → **Cursor** |

## 9. Env Vars & Risks
**Env vars:** `ANTHROPIC_API_KEY` — Vercel env var / `.env.local`, never committed.

**Open risks:**
- `reference_creatives` sometimes returns 3–4 items instead of 5 (S4 diversion, deferred —
  revisit if it recurs after S6b's prompt tightening)
- `--c-*-soft` opacity values were single-value picks from a spec range (e.g. green-soft
  0.12 from a 0.05–0.16 range) — flag during S6a if any look off
- Loading copy ("~4 seconds to generate") is inaccurate now that the truncation fix means
  verbose briefs can take 60–100s+ — revisit during visual polish (see
  docs/KNOWN_ISSUES.md Notes)